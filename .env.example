# =============================================================================
# ALORA MARKETING SITE - ENVIRONMENT CONFIGURATION
# =============================================================================
#
# This file serves as a template for .env.local (local development).
# Copy this file to .env.local and configure your values.
#
# VERCEL DEPLOYMENT:
#   All variables marked [VERCEL] must also be added to Vercel:
#   Project Settings → Environment Variables → Add for all environments
#
# =============================================================================


# =============================================================================
# PASSWORD PROTECTION
# =============================================================================
# [VERCEL] Add to Vercel Environment Variables

# Enable/disable password protection
# Set to 'true' to require password, 'false' to make site public
ENABLE_PASSWORD_PROTECTION=true

# The password required to access the site (only used when protection is enabled)
# Change this to your own secure password
SITE_PASSWORD=your-secret-password-here


# =============================================================================
# CONTACT FORM - EMAIL (Resend)
# =============================================================================
# [VERCEL] Add to Vercel Environment Variables
#
# Resend is used to send contact form submissions via email.
# Emails are sent FROM noreply@contact.aloraops.com TO contact@aloraops.com
#
# Setup:
#   1. Create account at https://resend.com
#   2. Add and verify domain: contact.aloraops.com (subdomain)
#   3. Create API key at https://resend.com/api-keys
#      - Permission: Sending access
#      - Domain: contact.aloraops.com (scoped for security)
#
# Free tier: 100 emails/day, 3,000 emails/month
# Dashboard: https://resend.com/emails (view sent emails)

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Where contact form submissions are sent (can be any email address)
# Change this to route submissions to a different inbox
CONTACT_FORM_RECIPIENT=contact@aloraops.com


# =============================================================================
# CONTACT FORM - RATE LIMITING (Upstash Redis)
# =============================================================================
# [VERCEL] Add to Vercel Environment Variables
#
# Upstash provides serverless Redis for rate limiting contact form submissions.
# This prevents spam and abuse (max 3 submissions per IP per hour).
#
# Setup:
#   1. Create account at https://console.upstash.com
#   2. Create a new Redis database:
#      - Name: alora-marketing-ratelimit
#      - Type: Regional
#      - Region: us-east-1 (matches Vercel/Resend)
#   3. Copy the REST URL and Token from the database details page
#
# Free tier: 10,000 requests/day
# Dashboard: https://console.upstash.com (view rate limit analytics)

UPSTASH_REDIS_REST_URL=https://xxxx-xxxx-xxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
